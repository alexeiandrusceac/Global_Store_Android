package md.globalstoremd.com.webinteraction;

import md.globalstoremd.com.entities.User;

public class Authentication  {

    WebInteraction  webInteraction;
    public Authentication()
    {
        webInteraction = new WebInteraction();
    }
    public boolean login(String usr_name, String passwd)
    {
      //  webInteraction.login(usr_name,passwd);

        return false;
    }
    public boolean register_user(User user)
    {

        return false;
    }
}
